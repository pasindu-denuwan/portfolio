{
  "name": "portfolio-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix portfolio-main",
    "build": "npm run build --prefix portfolio-main",
    "start": "npm run start --prefix portfolio-main",
    "lint": "npm run lint --prefix portfolio-main"
 
  }
  
}
